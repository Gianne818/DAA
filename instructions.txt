
List* mergeSort(List* list){
    if(list->size()<=1){
        return list;
    }

    //divide the list and populate
    LinkedList* firsthalf = new LinkedList();
    LinkedList* secondhalf = new LinkedList();


    //sort each half
    cout << "My first half: ";
    firsthalf->print();
    firsthalf = mergeSort(firsthalf);
    cout << "My second half: ";
    secondhalf->print();
    secondhalf = mergeSort(secondhalf);

    //merge each half
    LinkedList* sorted = new LinkedList();

    sorted->print();
    return sorted;

}


void print(){
    node* temp = head;
    while(temp){
        if(temp!=tail){
            cout << temp->element << " -> ";
        } else {
            cout << temp->element;
        }
        temp = temp->next;
    }
}
